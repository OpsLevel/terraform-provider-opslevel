## v1.12.0 - [July 23, 2026](https://github.com/OpsLevel/terraform-provider-opslevel/compare/v1.11.1...v1.12.0)
### Fixed
* Fixed nil-pointer dereference in `opslevel_filter` read when state holds a `case_sensitive` value for a predicate (e.g. `exists`, `matches` against an ID) that the OpsLevel API returns as `nil` — previously caused `terraform plan` to crash for workspaces upgrading from 0.8.x state
### Added
* New `opslevel_integration_kubernetes` resource for managing a Kubernetes integration's ETL config (`extract_definition` / `transform_definition`) as code